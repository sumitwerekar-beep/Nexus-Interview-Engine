{
  "name": "nexus-interview-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.0",
    "express": "^4.18.2",
    "groq-sdk": "^1.1.2",
    "multer": "^2.1.1"
  }
}
